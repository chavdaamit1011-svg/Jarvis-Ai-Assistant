import { z } from 'zod';
export const utilitySchema=z.discriminatedUnion('action',[z.object({action:z.literal('calculate'),expression:z.string().min(1).max(200)}),z.object({action:z.literal('percentage'),amount:z.number().finite(),percentage:z.number().finite()}),z.object({action:z.literal('gst'),amount:z.number().finite().min(0),percentage:z.number().finite().min(0).max(100)}),z.object({action:z.literal('discount'),amount:z.number().finite().min(0),percentage:z.number().finite().min(0).max(100)}),z.object({action:z.literal('current_time'),timezone:z.string().max(100).optional()}),z.object({action:z.literal('current_date'),timezone:z.string().max(100).optional()})]);
