const secret=/(api[_-]?key|authorization|password|mongodb|token|embedding|system prompt)/i;export function sanitizeTrace(value:unknown):unknown{if(typeof value==='string')return value.length>500?`${value.slice(0,500)}…`:value;if(Array.isArray(value))return value.slice(0,30).map(sanitizeTrace);if(value&&typeof value==='object')return Object.fromEntries(Object.entries(value as Record<string,unknown>).filter(([key])=>!secret.test(key)).map(([key,item])=>[key,sanitizeTrace(item)]));return value;}
