import { z } from 'zod';
export const capabilityRouteSchema = z.object({ capability: z.enum(['knowledge','general_ai','utility','web_search','file','clarification','unsupported']), intent: z.string().max(100), entities: z.array(z.object({ type: z.string().max(50), name: z.string().max(180) })).max(10), requestedOperation: z.string().max(100).nullable(), requiresCurrentInformation: z.boolean(), requiresKnowledgeSearch: z.boolean(), requiresClarification: z.boolean(), clarificationQuestion: z.string().max(300).nullable(), confidence: z.number().min(0).max(1), reasonCode: z.string().max(100), usedAiRouting: z.boolean().optional(), fallbackUsed: z.boolean().optional() });
