import { z } from 'zod';
export const capabilityRouteSchema = z.object({ capability: z.enum(['knowledge','general_ai','utility','web_search','file','clarification','unsupported']), intent: z.string().max(100), entities: z.array(z.object({ type: z.string().max(50), name: z.string().max(180), id:z.string().optional() })).max(10), matchedEntity:z.string().nullable(),matchedEntityId:z.string().nullable(),entityMatchType:z.enum(['full_name','alias','weak','none']), requestedOperation: z.string().max(100).nullable(), requiresCurrentInformation: z.boolean(),requiresFreshInformation:z.boolean(), requiresKnowledgeSearch: z.boolean(), requiresClarification: z.boolean(), clarificationQuestion: z.string().max(300).nullable(), confidence: z.number().min(0).max(1), reasonCode: z.string().max(100), usedAiRouting: z.boolean().optional(), fallbackUsed: z.boolean().optional() });
